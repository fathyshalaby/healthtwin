// Entry for the self-contained browser bundle: registers the custom element on load.
import { defineHealthTwinCapture } from "./embedElement";

defineHealthTwinCapture();

export { defineHealthTwinCapture };
